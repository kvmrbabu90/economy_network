# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# EconGraph artifacts (cached filings, pipeline outputs, sqlite db)
data/
!data/.gitkeep
*.db
*.sqlite
*.sqlite3

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Web frontend (Phase 6)
web/node_modules/
web/dist/
# Local preview-tool launch config; the contents are environment-specific.
.claude/

# Secrets
.env
.env.*
!.env.example
!web/.env.development

# SQLite WAL / shared-memory sidecars (the .db itself is already ignored)
*.db-shm
*.db-wal

# Logs (incl. stray redirect artifacts like `logsapi.log`, `..logsweb.log`)
*.log
logs/

# Superpowers / agent scratch
.superpowers/

# Local scratch — ad-hoc debug scripts and impact/test dumps
check_nio.py
*_test.json
# A/B harness local LLM-response cache (machine-specific, not shared)
scripts/.ab_cache.json
